import styled from 'styled-components';

import { H2 } from '../components/Typography';

export const WidgetTitle = styled(H2).attrs({ expand: true, textAlign: 'left' })`
  color: white;
  font-weight: 400;
`;
